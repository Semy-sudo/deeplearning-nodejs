'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _fs = require('fs');

var _fs2 = _interopRequireDefault(_fs);

var _path = require('path');

var _path2 = _interopRequireDefault(_path);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/** pose.remove
 * @desc Remove a template
 * @param {Object} opts - Options.
 */
function remove() {
  var opts = arguments.length <= 0 || arguments[0] === undefined ? {} : arguments[0];

  var name = opts.name || _path2.default.basename(process.cwd());
  var template = _path2.default.join(opts._templates, name + '.tar');

  process.stdout.write('Are you sure you want to remove ' + name + ' [y/N]? ');

  process.stdin.resume();
  process.stdin.on('data', function (rawInput) {
    var input = rawInput.toString().trim().toLowerCase();

    if (input === 'y' || input === 'yes') {
      _fs2.default.unlink(template, function (err) {
        if (!err) {
          console.log('Removed ' + name + '.');
        } else {
          console.error('Failed to remove ' + name + '.');
        }
        process.stdin.pause();
      });
    } else if (input === 'n' || input === 'no' || !input) {
      console.error('Aborting.');
      process.stdin.pause();
    } else {
      console.error('Invalid input ' + input + '.  Aborting.');
      process.stdin.pause();
    }
  });
}

exports.default = remove;