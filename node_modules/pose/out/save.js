'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _fs = require('fs');

var _fs2 = _interopRequireDefault(_fs);

var _path = require('path');

var _path2 = _interopRequireDefault(_path);

var _fstream = require('fstream');

var _tar = require('tar');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/** pose.save
 * @desc Save a template.
 * @param {Object} opts - Options.
 */
function save() {
  var opts = arguments.length <= 0 || arguments[0] === undefined ? {} : arguments[0];

  var context = opts.context || process.cwd();
  var name = opts.name || _path2.default.basename(context);
  var template = _path2.default.join(opts._templates, name + '.tar');

  var dest = _fs2.default.createWriteStream(template);
  var pack = new _tar.Pack({ noProprietary: true });
  var read = new _fstream.Reader({ path: _path2.default.resolve(context), type: 'Directory' }).pipe(pack).pipe(dest);

  pack.on('end', function () {
    console.log('Saved ' + context + ' as ' + name + '.');
  });

  pack.on('error', function () {
    console.log('Failed to pack ' + template);
  });

  read.on('error', function () {
    console.log('Failed to read ' + context + '.');
  });
}

exports.default = save;