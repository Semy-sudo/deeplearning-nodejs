'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _fs = require('fs');

var _fs2 = _interopRequireDefault(_fs);

var _path = require('path');

var _path2 = _interopRequireDefault(_path);

var _tar = require('tar');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/** pose.init
 * @desc Initiate a template.
 * @param {Object} opts - Options
 */
function init() {
  var opts = arguments.length <= 0 || arguments[0] === undefined ? {} : arguments[0];

  var name = opts.name || _path2.default.basename(process.cwd());
  var context = opts.context || _path2.default.join(process.cwd(), name);

  var template = _path2.default.join(opts._templates, name + '.tar');

  var extract = new _tar.Extract({ path: context });
  var write = _fs2.default.createReadStream(_path2.default.resolve(template)).pipe(extract);

  extract.on('end', function () {
    console.log('Initiated ' + name + ' as ' + context + '.');
  });

  extract.on('error', function () {
    console.log('Failed to extract ' + name);
  });

  write.on('error', function () {
    console.log('Failed to write ' + context);
  });
}

exports.default = init;